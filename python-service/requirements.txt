fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
openai==1.59.9

# NLP & ML
# torch CPU-only is installed separately in Dockerfile (no CUDA needed on Railway)
sentencepiece==0.2.1
vaderSentiment==3.3.2
keybert==0.8.5
sentence-transformers==3.3.1

# Audio transcription
# openai-whisper==20231117  # not needed — service uses OpenAI Whisper API via openai SDK
yt-dlp==2025.1.15        # for downloading media for transcription

# Scraping (public data only — httpx + HTML parsing, no browser needed)
beautifulsoup4==4.12.3
lxml==5.3.0

# Utilities
python-multipart==0.0.20
python-dotenv==1.0.1
structlog==24.4.0
tenacity==9.0.0
